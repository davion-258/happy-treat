pluginManagement {
    repositories {
        gradlePluginPortal()  // Recommended default
    }
}

rootProject.name = 'happy-treat'

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        mavenCentral()
    }
}